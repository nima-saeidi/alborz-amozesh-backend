# Alborz Amozesh Backend

Django backend for Alborz School management system.

## Features
- PostgreSQL database
- Environment variables via `.env`
- Configured `.gitignore`
- Ready for production separation

## How to run
```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
